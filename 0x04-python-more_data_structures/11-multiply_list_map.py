#!/usr/bin/python3
# Auth: Dtechguru
# a new func is to be defined here now (list[])12342684062
def multiply_list_map(my_list=[], number=0):
    """next is the return statement"""
    return (list(map((lambda i: i * number), my_list)))
