#!/usr/bin/python3
# Auth: Dtechguru

# Now lets take our time creating our own dimentional and beatiful func
def simple_delete(a_dictionary, key=""):
    # an if statement now
    if a_dictionary.get(key) is not None:
        # dekl asin del here i npython (Dtechguru)
        del a_dictionary[key]
    return (a_dictionary)
# Dtechguru
