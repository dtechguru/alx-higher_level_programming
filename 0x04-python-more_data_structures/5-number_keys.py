#!/usr/bin/python3
# Auth: Dtechguru
def number_keys(a_dictionary):
    num = 0
    list_keys = list(a_dictionary.keys())
# a for loop here now (num)
    for i in list_keys:
        num += 1
# return statement return func(num)
    return (num)
