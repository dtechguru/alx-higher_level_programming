#!/usr/bin/python3
# Auth: Dtechguru

# or new func "(a_dic)"
def best_score(a_dictionary):
    if not a_dictionary:

        return (None)

# Dtecguru
    return (max(a_dictionary, key=a_dictionary.get))  # return statement{techy}
