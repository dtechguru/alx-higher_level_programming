#!/usr/bin/python3
# Auth: Dtechguru

# Defining a func here name (a_dictionary, value, key) dtechguru
def update_dictionary(a_dictionary, key, value):
    """exchange or add value key to ...."""
    a_dictionary[key] = value
    # return (a_dictionary)
    return (a_dictionary)
# Dtechguru
