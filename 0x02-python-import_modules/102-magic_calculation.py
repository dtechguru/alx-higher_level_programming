#!/usr/bin/python3
# trust me Pythin feels like magic

# "Dtechguru" rocking python yh
def magic_calculation(a, b):
    # our code performs simple calcu "Dtechguru"
    """Match bytecode provided by Holberton School."""
    # we used from her another function "Dtechguru"
    from magic_calculation_102 import add, sub

# an if statement "Dtechguru"
    if a < b:
        c = add(a, b)
        for i in range(4, 6):
            c = add(c, i)
        return (c)

#  this is magic right
    else:
        return(sub(a, b))
