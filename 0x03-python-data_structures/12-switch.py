#!/usr/bin/python3
# Dtechguru
# we'll do something very basic in this program
# we'll just assign integers to a variable
a = 89  # Assignment
b = 10  # Assignment to int 10 in var b
a, b = b, a  # it's what it is
# then i will print using a new format not create but also builtin
print("a={:d} - b={:d}".format(a, b))  """Dtechguru"""
